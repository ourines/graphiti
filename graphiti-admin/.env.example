VITE_GRAPHITI_PROXY_TARGET=http://localhost:8000
VITE_BACKUP_PROXY_TARGET=http://localhost:8080

# Optional overrides for deployments where the admin app is served from a
# different origin than the APIs. When unset, the app will call
# /graphiti-api and /backup-api so you can front them with a reverse proxy.
# VITE_GRAPHITI_API_URL=https://your-domain.com/graphiti-api
# VITE_BACKUP_API_URL=https://your-domain.com/backup-api
